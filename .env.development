# Development environment variables
# VITE_REDIRECT_URL is optional - defaults to window.location.origin (http://localhost:8080)
# Uncomment and set only if you need to override the redirect URL for local development
# VITE_REDIRECT_URL=http://localhost:8080
